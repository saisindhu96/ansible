-  name: Demo on Variables
   hosts: all
   vars:
     URL: play.example.com
   tasks:
     -  name: Print Variable
        debug:
          msg: URL = {{ URL }}
     -  name: Print variables from task
        vars:
          URL: task.example.com
        debug:
          msg: URL = {{ URL }}